apiVersion: v1
kind: Service
metadata:
  name: minio-proj
  namespace: minio-ns
  labels:
    app: minio
spec:
  type: ClusterIP
  ports:
    - name: api
      port: 9000
      targetPort: api
    - name: console
      port: 9090
      targetPort: console
  selector:
    app: minio
